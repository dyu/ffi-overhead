include_rules
# =====================================

: foreach *.c |> !cc |>

# ===================================== exe linked to shared lib

: *.o | newplus/libnewplus.so |> gcc %f -o %o -Lnewplus -lnewplus -Wl,-rpath,'$ORIGIN/newplus' |> c_hello

